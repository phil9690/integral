.list-item-container
  .list-item
    - formatted_item = Integral::ListItemRenderer.new(f.object)
    = render 'item_modal', f:f, formatted_item: formatted_item
    .col.m1
      %i.material-icons.identifier web
    .data.col.m9
      %span.title= formatted_item.title
      %span.url= formatted_item.url
    .actions.col.m2.center
      = link_to "#list-item-modal-#{f.object.id}", class: 'modal-trigger' do
        %i.material-icons.action edit

      .add-children
        = link_to_add_association f, :children, 'data-association-insertion-method' => 'prepend' do
          %i.material-icons.action add

  .children.sortable.hide
    = f.simple_fields_for :children do |list_item|
      = render 'child_fields', f: list_item

