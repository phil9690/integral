<%= grid(@menus_grid) do |g|
  g.column name: t('.columns.title'), attribute: 'title'
  g.column name: t('.columns.description'), attribute: 'description'

  g.column do |menu|
    # link_to("<span class='material-icons green-text text-lighten-1'>remove_red_eye</span>".html_safe, page_path(page)) +
      link_to("<span class='material-icons blue-text text-lighten-1'>edit</span>".html_safe, menu_path(menu)) +
      link_to("<span class='material-icons red-text text-lighten-1'>delete</span>".html_safe, menu_path(menu), method: :delete, data: { confirm: t('integral.actions.confirm') })
  end

  g.blank_slate  do
    "<div><br />#{t('integral.menus.index.non_available')}</div>"
  end
end -%>

